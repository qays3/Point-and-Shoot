Win
```ps
cd backend
python -m venv myenv
.\myenv\Scripts\activate
pip install -r .\requirements.txt
```

Linux
```bash
cd backend
python3 -m venv myenv
source myenv/bin/activate
pip install -r requirements.txt
```
